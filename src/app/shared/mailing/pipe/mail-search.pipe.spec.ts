import { MailSearchPipe } from './mail-search.pipe';

describe('MailSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MailSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
