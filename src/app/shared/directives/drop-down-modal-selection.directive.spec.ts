import { DropDownModalSelection} from './drop-down-modal-selection.directive';

describe('DropDownModalSelectionDirective', () => {
  it('should create an instance', () => {
  //  const directive = new DropDownModalSelection();
    //expect(directive).toBeTruthy();
  });
});
 